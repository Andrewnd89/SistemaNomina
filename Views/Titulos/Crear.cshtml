@model SistemaNomina.Models.Title
@{
    ViewData["Title"] = "Nuevo Título";
}

<h2>Nuevo Título / Cargo</h2>
<form method="post">
    <div class="mb-3">
        <label>Empleado</label>
        <select name="emp_no" class="form-control">
            @foreach (var e in ViewBag.Empleados)
            {
                <option value="@e.emp_no">@e.first_name @e.last_name</option>
            }
        </select>
    </div>
    <div class="mb-3">
        <label>Título / Cargo</label>
        <input asp-for="title" class="form-control" />
    </div>
    <div class="mb-3">
        <label>Fecha Inicio</label>
        <input asp-for="from_date" type="date" class="form-control" />
    </div>
    <button type="submit" class="btn btn-success">Guardar</button>
    <a href="/Titulos" class="btn btn-secondary">Cancelar</a>
</form>